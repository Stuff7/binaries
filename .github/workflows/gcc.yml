name: Install GCC and Create Binaries Branch

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Print Possible Install Paths
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git checkout -b binaries
          echo MOVED TO BINARIES BRANCH
          mkdir temp
          cp -r "C:\msys64\mingw64\*" temp/
          mv temp/* .
          echo IN BINARIES BRANCH BEFORE ADD
          dir
          git status
          git add -A
          echo IN BINARIES BRANCH AFTER ADD
          git status
          git commit -m "Add GCC binaries"
          git status
          git push -f origin binaries
